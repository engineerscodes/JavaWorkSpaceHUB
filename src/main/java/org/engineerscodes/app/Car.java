package org.engineerscodes.app;

public final class Car implements DataLake, allowed {

    @Override
    public void sendMsg(allowed obj) {
        if(obj instanceof Car car){
           //ToDO
        }
        else{
            //TODO
        }
    }
}
